# تعليمات وكلاء التطوير — حصاد

ابدأ دائماً بقراءة `README.md` ثم الوثيقة المناسبة تحت `docs/`. هذا المستودع منصة زراعية سعودية تستخدم React وTypeScript وTailwind وtRPC وExpress وDrizzle مع MySQL/TiDB.

## قواعد لا تقبل الاستثناء

1. العربية هي لغة الواجهة الافتراضية RTL، والإنجليزية LTR. لا تكسر أيّاً من الاتجاهين.
2. لا تضف بيانات تجريبية على أنها إنتاجية، خصوصاً التقييمات أو الشهادات أو آراء العملاء أو أرقام الأداء.
3. لا تضف أسراراً أو مفاتيح API أو ملفات `.env` إلى Git.
4. لا تطبق ترحيلاً مدمراً. حدّث `drizzle/schema.ts` ثم أنشئ الترحيل وراجعه قبل تطبيقه.
5. لا تستخدم `git reset --hard` ولا تحذف بيانات المستخدمين أو الملفات المخزنة دون موافقة صريحة.
6. لا تنشئ عناصر `<a>` متداخلة؛ مكوّن `Link` في Wouter يرسم رابطاً بذاته.
7. الميزات الدائمة تحتاج طبقة بيانات وإجراء tRPC واختباراً؛ لا تعتمد على حالة واجهة محلية وحدها.

## خريطة مختصرة

| المجال | المسار الأساسي |
| --- | --- |
| المسارات والواجهة | `client/src/App.tsx` و`client/src/pages/` |
| المكوّنات والسياق | `client/src/components/` و`client/src/contexts/` |
| العقود والإجراءات | `server/routers.ts` |
| استعلامات قاعدة البيانات | `server/db.ts` |
| مخطط وترحيلات القاعدة | `drizzle/schema.ts` و`drizzle/` |
| الاختبارات | `server/*.test.ts` |
| الترجمة | `client/src/i18n/english.json` و`LanguageContext.tsx` |

## دورة التعديل المطلوبة

1. افهم المسار الحالي وابقَ ضمن أصغر نطاق ممكن.
2. عند تعديل البيانات اتبع: **schema → migration → database helper → tRPC router → UI → test**.
3. استخدم `trpc.*.useQuery` و`trpc.*.useMutation` بدلاً من طلبات HTTP مباشرة في الواجهة.
4. أضف حالات تحميل وفراغ وخطأ قابلة للفهم، وتحقق من الجوال وRTL.
5. شغّل `pnpm check && pnpm test` قبل التسليم، و`pnpm build` بعد الحزم أو إعدادات البناء.

راجع `docs/DEVELOPMENT_GUIDE.md` لإجراءات التشغيل و`docs/ARCHITECTURE.md` قبل تعديل المسارات أو الصلاحيات أو قاعدة البيانات.
